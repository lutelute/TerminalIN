# 引き継ぎ: TiN (TerminalIN)

> 作成日時: 2026-04-12 20:00
> セッションID: v1215-stable
> ステータス: 安定 — beta ブランチで追加修正中

---

## 1. 現在のブランチ状態

- **main**: v1.2.15 リリース済み (workspace色バッジ + daemon MacOS配置)
- **beta/grid-resize-fix**: #4 修正済み (overlay リサイズ保持)、dev 確認済み、未マージ

---

## 2. 今回のセッションで完了したこと (v1.2.12 → v1.2.15)

### パフォーマンス (最重要)
- **daemon を `Contents/MacOS/` に配置** → TCC 権限継承、パッケージ版 snap 8-56ms
- `atomicWriteJSON` 非同期化、`findWorkspace` O(1)、poll identity skip
- sidebar ドラッグ: fire-and-forget + 16ms throttle + ドラッグ中 poll スキップ
- daemon に `positionOnly` モード追加 (AX 呼出 3→1回)

### UI
- sidebar 白背景 (`backgroundColor: '#ffffff'`)
- 非アクティブ時: CSS `::after` 灰色マスク (`rgba(120,120,130,0.18)`)
- workspace 固有色 (blue/purple/green/orange/red/teal)
- 「→ xxx」バッジに他 workspace の色表示
- GRID タイトル + titlebar 下線にアクセント色
- snapped ウィンドウのタイトル表示バグ修正

### beta/grid-resize-fix (#4)
- overlay リサイズが sidebar 操作でリセットされない
- overlay resize → gridWidth/gridHeight 即時更新 + retile
- sidebar 移動時は overlay の位置のみ追従

### ビルドインフラ
- `scripts/build-daemon.sh` — 条件付きコンパイル
- `scripts/install.sh` — daemon inode 保持
- `.claude/CLAUDE.md` — TCC 知見記録

---

## 3. Open Issues

- **#4** grid overlay リサイズ改善 — beta で修正済み、マージ待ち
- **#5** workspace ごとディスプレイ移動 — 未着手

---

## 4. 次のアクション

1. beta/grid-resize-fix を main にマージ → v1.2.16 リリース
2. #5 ディスプレイ移動機能
3. 自動 snap グループ機能 (AI クラスタ化) — ユーザーが言及、Issue 未作成

---

## 5. コンテキスト・メモ

- ユーザーは「基本 Electron (dev) で開発」→ dev で確認してからパッケージ版
- 動作の一体感が最重要。raise-all / retile を削ると UX が壊れる
- AtelierX は競合していない
- daemon は `Contents/MacOS/` 配置が必須 (TCC 継承)
- `rm -rf` インストールは禁止 → `scripts/install.sh` を使う
