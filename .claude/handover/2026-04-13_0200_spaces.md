# 引き継ぎ: TiN (TerminalIN)

> 作成日時: 2026-04-13 02:00
> ステータス: 安定 (Spaces 移動は experimental)

---

## ブランチ: main (435dba5)

## セッション成果
- Auto Snap (AI): Claude CLI、一時ファイルプロンプト、AIボタン
- プリセット保存・復元 (テキスト入力ダイアログ、IME対応)
- バッジクリック → workspace 切替
- TERMINAL/FINDER 表示トグル
- コンソール固定タブ
- 起動復元一括化 (32win 459ms)
- daemon バナー完全削除
- raiseAll 条件付き app.focus
- Cmd+R osascript 強制 retile (#7)
- Cmd+Shift+→/← Spaces 移動 (experimental、プライベート API)
- install.sh 完全停止 + 速度自動確認
- IME 変換中 Enter 無視

## ⚠ 不安定な機能
- **Spaces 移動** (CGSMoveWindowsToManagedSpace): プライベート API、sidebar は手動移動必要、snap が外れる場合あり。安定化するか revert するか要判断

## 未着手
- コンソールから Claude Code 呼び出し
- Spaces 移動の安定化 or revert

## 注意
- daemon リビルドで CDHash 変更 → Contents/MacOS/ 配置で TCC 継承されるはずだが要確認
- dev ↔ パッケージ切替前にプリセット保存
- poll 負荷: workspace 数に比例、使わない ws は閉じる
